version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.8
  build:
    commands:
       - echo "MVN Build Started"
       - mvn -DskipTests clean install
  attifacts:
    files:
      - 'target/*.jar'
