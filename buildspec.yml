phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - mvn clean install
  build:
    commands:
      - mvn package

artifacts:
  files:
    - 'target/*.jar'
  name: myBuildArtifact