version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - mvn -DskipTests clean install
  build:
    commands:
      - mvn -DskipTests package

artifacts:
  files:
    - 'target/*.jar'
  name: myBuildArtifact