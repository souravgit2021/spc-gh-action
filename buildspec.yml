version: 0.2

phases:
   install: 
       runtime-versions:
           java: corretto17

   build: 
     commands:
         - mvn clean install -DskipTests
         - echo "The Value Of Parameter is $MY_Param"
         - echo "My Name Is = $Name"
